{
  "name": "dot-diary",
  "private": true,
  "scripts": {
    "test:sync": "node --test tests/*.test.mjs",
    "test:e2e": "playwright test",
    "test:all": "npm run test:sync && npm run test:e2e"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2"
  }
}
